<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- Database connection settings -->
        <property name="connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="connection.url">jdbc:mysql://localhost:3306/ptitsocialchat?useUnicode=true&amp;characterEncoding=utf8&amp;serverTimezone=UTC</property>
        <property name="connection.username">root</property>
        <property name="connection.password">123456</property>

        <!-- JDBC connection pool (use the built-in) -->
        <property name="connection.pool_size">5</property>

        <!-- SQL dialect -->
        <property name="dialect">org.hibernate.dialect.MySQL8Dialect</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="show_sql">true</property>
        
        <!-- Format SQL for better readability -->
        <property name="format_sql">true</property>

        <!-- Drop and re-create the database schema on startup -->
        <property name="hbm2ddl.auto">update</property>

        <!-- Names the annotated entity classes -->
        <mapping class="com.ptit.socialchat.model.User"/>
        <mapping class="com.ptit.socialchat.model.Post"/>
        <mapping class="com.ptit.socialchat.model.PostLike"/>
        <mapping class="com.ptit.socialchat.model.Comment"/>
        <mapping class="com.ptit.socialchat.model.FriendRequest"/>
        <mapping class="com.ptit.socialchat.model.Friendship"/>
        <mapping class="com.ptit.socialchat.model.Message"/>
        <mapping class="com.ptit.socialchat.model.ModerationSettings"/>
        <mapping class="com.ptit.socialchat.model.Announcement"/>

    </session-factory>
</hibernate-configuration>
